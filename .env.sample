# =============================================================================
# SHERLOCK_SKY_3DSIM - 환경 설정 샘플 (템플릿)
# =============================================================================
#
# 📌 사용법:
#    1. 이 파일을 .env로 복사: cp .env.sample .env
#    2. 실제 환경에 맞게 값 수정
#    3. .env 파일은 절대 Git에 커밋하지 마세요!
#
# 📌 버전: 2.0.0
# 📌 작성일: 2026-01-16
# =============================================================================

# =============================================================================
# 🔑 SERVER PORTS (중앙 관리)
# =============================================================================
# 서버 이전 시 이 값들만 수정하세요!
BACKEND_PORT=8008
FRONTEND_PORT=8088
BACKEND_HOST=0.0.0.0
FRONTEND_HOST=0.0.0.0

# === API URLs ===
# 외부 접속 시 localhost를 서버 IP로 변경
# 예: API_BASE_URL=http://192.168.1.100:8008/api
API_BASE_URL=http://localhost:8008/api
WS_URL=ws://localhost:8008/ws

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
ENVIRONMENT=development
LOG_LEVEL=INFO
USE_MULTI_CONNECTION=false

# === Legacy Port Support (기존 호환성) ===
# APP_PORT는 BACKEND_PORT와 동일하게 유지
APP_PORT=8008
API_PORT=8008
API_HOST=0.0.0.0
API_RELOAD=true

# =============================================================================
# CORS SETTINGS
# =============================================================================
CORS_ORIGINS=http://localhost:8088,http://127.0.0.1:8088,*
ALLOWED_ORIGINS=http://localhost:8088,http://127.0.0.1:8088

# =============================================================================
# MULTI-SITE CONFIGURATION
# =============================================================================
DEFAULT_SITE=korea_site1
DEFAULT_DB_NAME=factory_line1_db
DATABASE_CONFIG_FILE=config/databases.json

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# settings.py용 (REMOTE_DB_*)
REMOTE_DB_HOST=your-db-server.example.com
REMOTE_DB_PORT=5432
REMOTE_DB_NAME=your_database
REMOTE_DB_USER=your_user
REMOTE_DB_PASSWORD=your_secure_password
DATABASE_TYPE=postgresql

# 레거시 호환 (DB_*)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sherlock_sky
DB_USER=postgres
DB_PASSWORD=change_this_password

# =============================================================================
# CONNECTION POOL SETTINGS
# =============================================================================
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_ECHO=false

# =============================================================================
# REDIS SETTINGS
# =============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# =============================================================================
# MONITORING SETTINGS
# =============================================================================
DATA_COLLECTION_INTERVAL=5
WEBSOCKET_INTERVAL=1

# =============================================================================
# SECURITY SETTINGS
# =============================================================================
API_KEY=your-api-key-here
JWT_SECRET=your-jwt-secret-key-here
